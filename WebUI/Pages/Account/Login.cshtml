@page
@using WebUI.Pages.Account
@model LoginModel
@{
    ViewData["Title"] = "Login";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - GoBike</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="login-page">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-sm-10 col-md-6 col-lg-5 col-xl-4">
                <div class="login-container p-5">
                    <div class="text-center mb-4">
                        <div class="brand-mark mx-auto mb-3">G</div>
                        <h1 class="h3 fw-bold mb-1">GoBike</h1>
                        <p class="text-muted mb-0">Motorcycle rental operations</p>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            @Model.ErrorMessage
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                    @if (TempData["RegistrationSuccess"] != null)
                    {
                        <div class="alert alert-success">@TempData["RegistrationSuccess"]</div>
                    }

                    <form method="post">
                        <div class="mb-3">
                            <label for="Input_Username" class="form-label">Username</label>
                            <div class="input-group">
                                <span class="input-group-text">User</span>
                                <input type="text" class="form-control" id="Input_Username" asp-for="Input.Username" placeholder="Enter your username" required>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label for="Input_Password" class="form-label">Password</label>
                            <div class="input-group">
                                <span class="input-group-text">Key</span>
                                <input type="password" class="form-control" id="Input_Password" asp-for="Input.Password" placeholder="Enter your password" required>
                            </div>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">Login</button>
                        </div>
                    </form>
                    <div class="d-flex justify-content-between mt-3 small">
                        <a asp-page="/Account/Register">Create customer account</a>
                        <a asp-page="/Motorcycle/Index">Continue as guest</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
